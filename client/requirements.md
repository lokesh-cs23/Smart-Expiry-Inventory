## Packages
date-fns | Date formatting and expiry calculations
recharts | Beautiful analytics charts
lucide-react | High-quality icons
clsx | Utility for constructing className strings conditionally
tailwind-merge | Utility for merging tailwind classes without style conflicts

## Notes
- Ensure all Wouter `<Link>` components are used directly without wrapping inner `<a>` tags.
- The `api.items.stats` endpoint is used for dashboard summary cards.
- Barcode lookup is triggered manually via the 'Lookup Barcode' button.
